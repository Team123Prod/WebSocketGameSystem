class Request {
    constructor(cmd, module, args) {
        this.cmd = cmd;
        this.module = module;
        this.args = args;
    }
}
//class Request {
//	constructor(cmd, module, idRoom, player, idMove) {
//    this.cmd = cmd;
//    this.module = module;
//	  this.idRoom = idRoom;
//	  this.player = player;
//	  this.idMove = idMove;
//  }
//}
